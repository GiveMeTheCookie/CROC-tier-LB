# Setup

1. Enable **Settings → Actions → General → Workflow permissions → Read and write permissions**.
2. Run **Actions → Scrape Clan Leaderboard → Run workflow** once.
3. Set `DATA_URL` in `clan_leaderboard.html` to:

   ```
   https://raw.githubusercontent.com/<user>/<repo>/main/data/leaderboard.json
   ```
